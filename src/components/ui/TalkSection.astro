---
export interface Props {
  title: string;
  subtitle: string;
}

const { title, subtitle } = Astro.props;
---

<section class="mb-12">
  <h2 class="text-2xl font-bold mb-2">{title}</h2>
  <p class="text-base-content text-sm mb-6">{subtitle}</p>
  <slot />
</section>
